.message_filter
  div(class="{{color}}")
    .guide
      h3.mesname
        a(name="{{logid}}") {{name}}
      p(class="{{style}} text") {{{text}}}
      p.mes_date  ({{logid}}) {{time}}
      hr.invisible_hr
